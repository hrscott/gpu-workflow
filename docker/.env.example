###############################################################################
# GPU Workflow Environment Configuration Example
# Copy this file to ".env" and edit as needed before running:
#   cp .env.example .env
###############################################################################

###############################################################################
# Docker Image Configuration
###############################################################################

# The Docker image to run for the main GPU application.
# This should correspond to the ColabDesign image built from colabdesign.Dockerfile.
IMAGE_REF=colabdesign-gpu:latest

# The command to run when the container starts.
# "sleep infinity" keeps the container alive quietly so you can attach with `docker exec`.
# For interactive debugging you can change this to "bash".
APP_COMMAND=sleep infinity

###############################################################################
# GPU and Container Resources
###############################################################################

# Number of GPUs to expose to the container.
# You can use a specific number (e.g. 1, 2) or "all".
GPU_COUNT=all

# Shared memory allocation for the container.
# ColabDesign and JAX benefit from higher SHM when using large models.
SHM_SIZE=16g

###############################################################################
# Mounted Directories (host → container)
###############################################################################
# These paths are relative to this "docker/" directory.

# Host directory containing user scripts or notebooks.
# Mounted as /workspace/code inside the container.
CODE_DIR=./code

# Host directory containing reference data, model parameters, or weights.
# Mounted read-only at /workspace/data.
DATA_DIR=./data

# Host directory where results and outputs will be written.
# Mounted read-write at /workspace/outputs.
OUTPUT_DIR=./outputs

###############################################################################
# Notes
###############################################################################
# - Edit these values in your own ".env" file before running `./run_pipeline.sh up`.
# - Make sure the directories listed above exist on the host:
#       mkdir -p docker/code docker/data docker/outputs
# - To rebuild the ColabDesign image if it doesn't exist:
#       ./scripts/build_colabdesign_image.sh
# - To open a shell inside the running container:
#       ./scripts/colabdesign_shell.sh
###############################################################################
